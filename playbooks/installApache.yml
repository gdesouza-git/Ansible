- name: Instalar Apache
  hosts: vm-003.localdomain
  become: yes
  tasks:
    - name: Instalar ultima versão do Apache
      apt:
        name: apache2
        name: ifconfig
        update_cache: yes
        state: present
    - name: Iniciar httpd
      service:
        name: apache2
        state: started
        enabled: true
        force: yes
